language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install -U coveralls 
install:
  - pip install .
script: pytest
after_success:
  - coverage run --omit=tests/* setup.py test
  - coveralls 